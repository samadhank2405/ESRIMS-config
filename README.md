# Spring Cloud Config repository

This repository holds external configuration for the microservices in the parent workspace.

Keep service source code, Docker files, IDE metadata, build output, and personal documents out of this repository. Add service configuration as root-level `.yml` or `.yaml` files, for example `product-service.yml`.
